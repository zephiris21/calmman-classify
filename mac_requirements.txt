# PyTorch 기본 패키지 (MPS 지원)
# 주의: PyTorch는 별도로 설치하는 것이 권장됨
# pip install torch torchvision torchaudio

# NumPy (2.0 미만 버전 필요)
numpy<2.0

# 컴퓨터 비전
opencv-python-headless>=4.7.0
Pillow>=9.4.0

# 얼굴 인식 (--no-deps 옵션으로 설치 필요)
# pip install --no-deps facenet-pytorch>=2.5.2

# 딥러닝 모델
timm>=0.6.12

# 데이터 처리
pandas>=1.5.0
scikit-learn>=1.2.0

# 이미지 증강
albumentations>=1.3.0

# 시각화
matplotlib>=3.6.0
seaborn>=0.12.0

# 진행률 표시
tqdm>=4.64.0

# 설정 파일
PyYAML>=6.0

# 유틸리티
pytubefix>=9.0.0 